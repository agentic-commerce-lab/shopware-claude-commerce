# Materialized at runtime by shopware_merchant_sdk.agent.make_options() through the
# blueprint's ensure_project_skills(): symlinks (or copies) of vendor/skills/merchant/* so
# the Agent SDK discovers them as project Agent Skills. Never edit these — the skills are
# vendored verbatim from anthropics/commerce-agents (see NOTICE).
.claude/
# This runtime's change queue (MERCHANT_SDK_LEDGER_DSN): staged payloads and their
# approval state, never committed.
.ledger.db
