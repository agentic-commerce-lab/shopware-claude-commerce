# Runtime state of the reference server, never committed: what the operator asked it to
# remember (MERCHANT_MCP_MEMORY_FILE moves it) and this server's own change queue with the
# staged Shopware payloads (MERCHANT_MCP_LEDGER_DSN moves it).
.merchant_memory.json
.ledger.db
