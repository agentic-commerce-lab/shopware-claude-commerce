# Runtime state of the reference server: whatever the demo customer asked it to remember
# (personal data in a real deployment), never committed. STOREFRONT_MCP_MEMORY_FILE moves it.
.storefront_memory.json
