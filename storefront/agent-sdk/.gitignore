# Materialized at runtime by shopware_shopping_sdk.agent.make_options() through the
# blueprint's ensure_project_skills(): symlinks (or copies) of vendor/skills/shopping/* so
# the Agent SDK discovers them as project Agent Skills. Never edit these — the skills are
# vendored verbatim from anthropics/commerce-agents (see NOTICE).
.claude/
