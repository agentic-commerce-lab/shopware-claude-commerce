<?xml version="1.0" encoding="UTF-8"?>
<!--
  Standalone unit tests for the handoff verifier. No kernel, no database.
  Run inside the shop container (uses the shop's vendor/bin/phpunit + autoloader):

    docker exec -u www-data commerce-agents-shopware bash -lc \
      'cd /var/www/html && php vendor/bin/phpunit -c custom/plugins/CommerceAgentsHandoff/phpunit.xml'
-->
<phpunit xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:noNamespaceSchemaLocation="https://schema.phpunit.de/11.5/phpunit.xsd"
         bootstrap="tests/bootstrap.php"
         colors="true"
         failOnRisky="true"
         failOnWarning="true"
         cacheDirectory="/tmp/commerce-agents-handoff-phpunit">
    <testsuites>
        <testsuite name="CommerceAgentsHandoff">
            <directory>tests</directory>
        </testsuite>
    </testsuites>
    <source>
        <include>
            <directory>src/Service</directory>
        </include>
    </source>
</phpunit>
